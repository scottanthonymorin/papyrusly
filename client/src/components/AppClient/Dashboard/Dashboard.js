import React from "react";
import styled from "styled-components";
import Data from "./Data/Data";

const DashBoard = () => {
  //will have horizontal tweet feed based on category obtained from state
  //will provide sentiment of tweet beside it + team affected
  //search bar to find all odds associated with team (redux)
  //access selected category from redux
  //hit backend endpoint while passing category in body
  //backend activates puppeteer to get odd values
  //values are stored in redux state
  //dashboard shows odds based on redux state odd values
  //odds showed based on largest percentage abr (backend calc)
  //

  return <Data />;
};

export default DashBoard;
